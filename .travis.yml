language: php

php:
  - 8.1
  - 8.2

os: linux
dist: jammy

before_script:
  - composer self-update
  - composer install

script:
  - mkdir -p build/logs
  - composer ci

after_script:
  - php vendor/bin/php-coveralls -v
